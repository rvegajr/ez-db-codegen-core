using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.ComponentModel.DataAnnotations.Schema;

namespace {{Namespace}}
{
    [Table("{{TableName}}")]
    public class {{ClassName}}
    {
        {{#each Properties}}
        {{#if IsPrimaryKey}}
        [Key]
        {{/if}}
        {{#if IsNullable}}
        [Required(AllowEmptyStrings = false)]
        {{/if}}
        public {{DataType}} {{PropertyName}} { get; set; }{{#if IsNullable}}?{{/if}}
        {{/each}}

        {{#each NavigationProperties}}
        public virtual {{RelatedEntityType}} {{PropertyName}} { get; set; }
        {{/each}}
    }
}
