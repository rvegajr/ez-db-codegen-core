using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.ComponentModel.DataAnnotations.Schema;
using Microsoft.EntityFrameworkCore;

namespace {{Namespace}}.Models
{
    [Table("{{TableName}}", Schema = "{{SchemaName}}")]
    public partial class {{EntityName}}
    {
        public {{EntityName}}()
        {
            {{#each Relationships}}
            {{#if (eq FromTableName ../TableName)}}
            {{ToTableName}} = new HashSet<{{ToTableName}}>();
            {{/if}}
            {{/each}}
        }

        {{#each Properties}}
        {{#if IsPrimaryKey}}
        [Key]
        {{/if}}
        {{#if IsNullable}}
        public {{DataType}}? {{PropertyName}} { get; set; }
        {{else}}
        public {{DataType}} {{PropertyName}} { get; set; }
        {{/if}}
        {{/each}}

        {{#each Relationships}}
        {{#if (eq FromTableName ../TableName)}}
        public virtual ICollection<{{ToTableName}}> {{ToTableName}} { get; set; }
        {{/if}}
        {{#if (eq ToTableName ../TableName)}}
        public virtual {{FromTableName}} {{FromTableName}} { get; set; }
        {{/if}}
        {{/each}}
    }
}
